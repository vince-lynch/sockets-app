{
  "name": "mock-md-server",
  "version": "1.0.0",
  "description": "mock market data server",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "docker-compose -f ./docker-compose.dev.yml up --force-recreate --build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.24.0",
    "express": "^4.17.1",
    "lodash.random": "^3.2.0",
    "socket.io": "^4.3.2"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
