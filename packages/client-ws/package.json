{
  "name": "client-ws",
  "version": "1.0.0",
  "description": "ws client",
  "scripts": {
    "start": "node src/index.js",
    "dev": "docker-compose -f ./docker-compose.dev.yml up --force-recreate --build",
    "test": "jest",
    "docker:test": "docker-compose -f ./docker-compose.dev.yml up client"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^8.3.0",
    "express": "^4.17.1",
    "socket.io-client": "^4.4.0"
  },
  "devDependencies": {
    "jest": "^27.3.1"
  }
}
